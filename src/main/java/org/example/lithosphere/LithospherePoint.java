package org.example.lithosphere;

import java.util.ArrayList;
import java.util.List;

public class LithospherePoint {

    List<PlatePoint> pointsOnPosition = new ArrayList<>();
    public void evaluate() {
        if (pointsOnPosition.size() == 0 ){
            //find closest plate
        }
        if (pointsOnPosition.size() > 1 ){
            //find decide which point to leave
        }
        //is 1 aka dont do anything
    }
}
